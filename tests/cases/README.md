Some test cases in this directory are ported from the rust-regex's testdata.
Those are marked by case comments, and their original names are kept.

Cases added specifically for Zig regex are marked as such.
