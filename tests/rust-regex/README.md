# Rust Regex Corpus Placeholder

This directory is reserved for additional suites generated from Rust regex
TOML corpora beyond Fowler.

When adding data here, include upstream source attribution and license files
(similar to `tests/fowler/data/`).
