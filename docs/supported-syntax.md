# Supported Syntax Source of Truth

The authoritative feature/support matrix now lives in: `tests/harness/capabilities.zig`

- `cap_backend_map`: backend support per capability.
- `Capability`: canonical capability keys.

This replaces the manually maintained table in this document until most syntax features are supported.
